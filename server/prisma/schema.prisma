generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Todo {
  completed_at DateTime?
  description  String?
  id           Int       @default(autoincrement()) @id
}